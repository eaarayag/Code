# Placeholder SOC ownership for the SOC model (nio_soc-a0-*).
# Format (same as scan_ownership.txt / tap_ownership.txt): Owner,partition_prefix
#
# The SOC partition list is not finalized yet. Until entries are added here,
# SOC partitions parsed from the SOC L2 regression report are shown at
# Partition Level with owner "UNKNOWN" (never dropped). Add one line per SOC
# partition prefix below to assign owners and enable partition/test_type
# splitting, e.g.:
#   Jane Doe,socpart_example_0
#
# This file is loaded and merged on top of the pipeline's main ownership file
# by both report_l2_scan.py and report_l2_tap.py.
